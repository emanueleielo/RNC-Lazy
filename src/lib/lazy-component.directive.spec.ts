import { LazyComponentDirective } from './lazy-component.directive';

describe('LazyComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyComponentDirective();
    expect(directive).toBeTruthy();
  });
});
